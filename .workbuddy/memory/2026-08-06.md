- 实现门店「员工销售排名」页面（shop/StaffRanking.vue），路径 `/shop/staff-ranking`。
- 方案 A：仅统计收银台（头号管家）人工服务数据，页面顶部已用 n-alert 明确说明口径，不包含机柜自助点播。
- 功能：时间维度筛选（本周/本月/上月/本季度/自定义）、店铺筛选、Top3 领奖台卡片、完整排名表格（支持排序）、导出 CSV。
- 表格字段：排名、收银员、工号、店铺、状态、收银笔数、收银总额、客单价、退款笔数、退款金额、赠送笔数、赠送金额、净收银额。
- 同步更新：`src/router/index.ts` 增加路由，`src/layouts/ShopLayout.vue` 在「数据报表」菜单组添加「员工销售排名」入口。
- 优化平台提现页面（platform/PlatformWithdraw.vue）提现记录统计标签：原仅显示「处理中笔数+占额」「本月成功笔数」，现改为三组标签完整展示「处理中 笔数/金额」「成功 笔数/金额」「失败 笔数/金额」，新增 failedCount、successAmount、failedAmount 三个 computed 属性。
