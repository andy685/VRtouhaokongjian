# 2026-04-30 工作记录

## Vercel 部署准备

- 创建了 `vercel.json`，配置 SPA 路由重写（所有路径回落到 `/index.html`）
- `npm run build` 因 TS 类型错误失败（NoticePush.vue、SystemNoticeInbox.vue 等多处），改用 `npx vite build` 跳过 tsc
- `npx vite build` 构建成功，dist 产出正常（3.4MB JS + 188KB CSS）
- Vercel CLI v52.2.1 已全局安装但未登录，需用户手动完成 `vercel login` 和 `vercel --prod`
